{% extends 'base.html.twig' %}

{% block body %}
    <h1>Userinfo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ userInfo.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ userInfo.name }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ userInfo.age }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ userInfo.number }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ userInfo.address }}</td>
            </tr>
            <tr>
                <th>Userid</th>
                <td>{{ userInfo.userId.id }}</td>
                <th>Userid</th>
                <td>{{ userInfo.userId.emailVerify }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('userinfo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('userinfo_edit', { 'id': userInfo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
